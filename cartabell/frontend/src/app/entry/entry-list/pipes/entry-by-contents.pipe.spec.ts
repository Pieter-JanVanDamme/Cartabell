import { EntryByContentsPipe } from './entry-by-contents.pipe';

describe('EntryByContentsPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryByContentsPipe();
    expect(pipe).toBeTruthy();
  });
});
