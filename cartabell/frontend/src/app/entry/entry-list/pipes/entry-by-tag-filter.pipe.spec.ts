import { EntryByTagFilterPipe } from './entry-by-tag-filter.pipe';

describe('EntryByTagFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryByTagFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
