import { EntryByTagColorFilterPipe } from './entry-by-tag-color-filter.pipe';

describe('EntryByTagColorFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new EntryByTagColorFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
