import { EntrySortPipe } from './entry-sort.pipe';

describe('EntrySortPipe', () => {
  it('create an instance', () => {
    const pipe = new EntrySortPipe();
    expect(pipe).toBeTruthy();
  });
});
